services:
  link-pilot-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: link-pilot-server
    ports:
      - "4312:4312"
    environment:
      HOST: 0.0.0.0
      PORT: 4312
    volumes:
      - ./server:/app/server
    restart: unless-stopped
